<Window x:Class="AnyStatus.Apps.Windows.Infrastructure.Mvvm.Windows.WindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        xmlns:local="clr-namespace:AnyStatus.Apps.Windows.Infrastructure.Mvvm.Windows"
        mc:Ignorable="d"
        FontSize="{DynamicResource FontSize}">
    <!--<WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" GlassFrameThickness="0" ResizeBorderThickness="5" CornerRadius="0"/>
    </WindowChrome.WindowChrome>-->
    <Window.Template>
        <ControlTemplate TargetType="{x:Type Window}">
            <Border>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.8" Color="#333" />
                </Border.Effect>
                <DockPanel Margin="10" Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
                    <local:TitleBarView x:Name="Toolbar" DockPanel.Dock="Top"/>
                    <AdornerDecorator>
                        <ContentPresenter/>
                    </AdornerDecorator>
                </DockPanel>
            </Border>
        </ControlTemplate>
    </Window.Template>
</Window>
